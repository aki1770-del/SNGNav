name: condition_aggregator
description: >-
  Source-neutral interface for severity-qualified meteorological-advisory
  aggregation. Defines Advisory event + AdvisoryProvider for per-source
  adapters. Pure Dart.
version: 0.0.11

# Published to pub.dev since 2026-05-03, after its promotion checklist
# passed in full and the release was authorised.
# Default publish target = https://pub.dev (no `publish_to:` line).

repository: https://github.com/aki1770-del/sngnav
issue_tracker: https://github.com/aki1770-del/sngnav/issues
topics:
  - aggregator
  - advisory
  - winter-driving
  - meteorology
  - driver-assistance

environment:
  sdk: ^3.10.0

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.1
