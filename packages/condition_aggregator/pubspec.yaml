name: condition_aggregator
description: >-
  Source-neutral interface for severity-qualified meteorological-advisory
  aggregation. Defines Advisory typed event + AdvisoryProvider interface
  consumed by per-source adapter packages (NWS / JMA / etc.). Pure Dart.
version: 0.0.1

# Deploy-graduated 2026-05-03 per FDD bylaws Rule 6 (i)-(vi) all-green
# + Komada-voice pre-authorization carried by the authoring spawn.
# Default publish target = https://pub.dev (no `publish_to:` line).

repository: https://github.com/aki1770-del/sngnav
issue_tracker: https://github.com/aki1770-del/sngnav/issues
topics:
  - aggregator
  - advisory
  - winter-driving
  - meteorology
  - driver-assistance

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.1
