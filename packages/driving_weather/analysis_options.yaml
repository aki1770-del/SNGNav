include: package:lints/recommended.yaml

# Strict analyzer modes: no implicit casts from `dynamic`, no inference that
# falls back to `dynamic`, and no raw generic types. The code already passes
# all three; the flags hold new contributions to the same standard.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
