include: package:lints/recommended.yaml

# Matches the monorepo floor (see packages/driving_conditions/analysis_options.yaml).
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
