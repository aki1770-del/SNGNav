include: package:lints/recommended.yaml

# Strict-mode flags mirror navigation_safety_core + navigation_safety_enums
# + navigation_safety_calibration (per SNGNav review 2026-04-25 P2 finding
# #12). V14 protection for future contributions.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
