name: navigation_lend_mode_experiment
description: >-
  Aviation-style lend-mode handoff for snow-zone navigation. Explore-phase;
  never published. Provides a state machine and observer interface for
  explicit transfer of safety-threshold responsibility between drivers
  on the same device for a single trip.
version: 0.0.1

publish_to: none

environment:
  sdk: ^3.10.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
