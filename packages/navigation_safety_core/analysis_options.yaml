

# Strict-mode flags: the analyzer reports implicit casts from dynamic, types it
# cannot infer, and generic types without type arguments, so new code cannot
# add them silently. The existing code passes all three.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
