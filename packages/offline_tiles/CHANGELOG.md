# Changelog

## 0.1.0

- Initial release.
- `OfflineTileManager` with route and region cache APIs.
- Resolver-backed `TileProvider` with MBTiles, lower-zoom, online, and placeholder fallbacks.
- Pure Dart `_core` exports for tile source type, coverage tier, and cache config.
