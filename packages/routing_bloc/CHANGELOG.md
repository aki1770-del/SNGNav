# Changelog

## 0.1.1

- Add explicit API Overview wording to README and align install instructions.
- Republish current example app and pub.dev discovery metadata.

## 0.1.0

- Initial release.
- `RoutingBloc` route request / loading / active / error lifecycle.
- `RouteProgressBar` and `ManeuverIcons` for glanceable route guidance UI.
- `_core` exports for routing state and route progress status.
- Engine-agnostic posture built on `routing_engine`.