include: package:lints/recommended.yaml

# Strict-mode flags, matching the sibling driving_conditions package.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
