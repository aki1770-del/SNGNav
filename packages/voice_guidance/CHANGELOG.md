# Changelog

## 0.1.0

- Initial release.
- Add `TtsEngine` abstraction for voice output providers.
- Add `FlutterTtsEngine` implementation backed by `flutter_tts`.
- Add `NoOpTtsEngine` fallback for test and headless contexts.
- Add `VoiceGuidanceConfig` model for runtime tuning.
