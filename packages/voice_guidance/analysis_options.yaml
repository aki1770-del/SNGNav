include: package:flutter_lints/flutter.yaml

# Strict-mode flags (per SNGNav review 2026-04-25 P2 finding #12).
# V14 protection for future contributions; codebase currently strict-clean.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
